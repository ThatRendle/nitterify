{
  "name": "nitterify",
  "version": "0.1.1",
  "description": "Redirect x.com status and article links to a Nitter instance (default xcancel.com).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "package": "npm run build && rm -f nitterify.zip && cd dist && zip -qr -X ../nitterify.zip . && cd ..",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.7.1",
    "@types/chrome": "^0.1.28",
    "typescript": "^5.9.3",
    "vite": "^8.1.4",
    "vitest": "^4.0.18"
  }
}
